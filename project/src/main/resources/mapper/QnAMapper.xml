<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

  <mapper namespace="hansung.cap.mapper.QnAMapper">
  
  	<insert id="insertQnA">
  		insert into qna(title,content,userId,time) values (#{title},#{content},#{userId},#{time})
  	</insert>
  	
  	<select id="queryAll" resultType="hansung.cap.project.QnAVO">
  		select * from qna
  	</select>
  	
  	<select id="search" resultType="hansung.cap.project.QnAVO">
  		select * from qna where title like #{s} or content like #{s} or userid like #{s} 
  	</select>
  	
  	<select id="read" resultType="hansung.cap.project.QnAVO">
  		select * from qna where seq like #{s}
  	</select>
  	<delete id="delete">
  		delete from qna where seq like #{s}
  	</delete>
  	
  	<update id = "modify">
  		update qna set title=#{title}, content=#{content}, userId=#{userId}, time = #{time} where seq like #{seq} 
  	</update>
  	
  	<select id="paging" resultType="hansung.cap.project.QnAVO">
  		<![CDATA[select * from qna where SEQ <=#{paging} order by SEQ desc limit 10;]]>
  	</select>
  	
  	<select id="countBoard" resultType="int">
  		SELECT COUNT(*) FROM qna
  	</select>
  	
  </mapper>